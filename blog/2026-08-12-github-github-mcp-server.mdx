---
title: "Hotspots on github/github-mcp-server: the 5 functions with the highest activity-weighted risk"
description: "A Hotspots analysis of github/github-mcp-server at commit eff4c3c, surfacing the top functions by activity-weighted risk score."
pubDate: 2026-08-12
tags: ["oss", "go", "refactoring", "code-health"]
draft: true
repo: "github/github-mcp-server"
commit: "eff4c3c041742426f417f7c2247b96bbf6d60b69"
language: "go"
hotspots_version: "1.34.0"
analyzed_at: "2026-08-12T04:43:47Z"
clone_depth: 25
hybrid_touch_threshold: 5
topRisk: 18.32
topFunction: "CreatePRApp"
topFile: "ui/src/apps/pr-write/App.tsx"
topPatterns: ["exit_heavy", "god_function", "complex_branching", "long_function", "deeply_nested"]
patternCounts: {complex_branching: 4, deeply_nested: 2, exit_heavy: 5, god_function: 5, long_function: 4, neighbor_risk: 1}
---

\{/* TODO: one paragraph — what this project is and why it's interesting */\}
Intro paragraph here.

The table below ranks functions by **activity-weighted risk** — a score that multiplies structural complexity by recent commit frequency. A function that is both hard to understand (high cyclomatic complexity) and actively changing is a higher priority than one that is complex but untouched. CC = cyclomatic complexity (independent execution paths); ND = max nesting depth; FO = fan-out (distinct callees).

## Top 5 Hotspots

| Function | File | Risk | CC | ND | FO |
|---|---|---|---|---|---|
| `CreatePRApp` | ui/src/apps/pr-write/App.tsx | 18.3 | 64 | 5 | 65 |
| `CreateIssueApp` | ui/src/apps/issue-write/App.tsx | 18.0 | 147 | 4 | 104 |
| `EditPRApp` | ui/src/apps/pr-edit/App.tsx | 17.3 | 55 | 4 | 63 |
| `minimalProjectFieldValue` | pkg/github/minimal_types.go | 17.1 | 19 | 3 | 17 |
| `addCommandFromTool` | cmd/mcpcurl/main.go | 16.1 | 11 | 6 | 27 |

## Large Repo Analysis

github-mcp-server is a large repository. To stay within memory constraints, this analysis used **hybrid touch mode**: structural complexity — CC, ND, FO — is measured precisely for every function. Git activity is tracked at the function level (via `git log -L`) only for files with 5 or more commits in the last 30 days; other files use a file-level approximation. Rankings therefore surface functions that are both structurally complex and in the most actively-changing parts of the codebase. Dormant code with high structural complexity will rank lower than it would under a full per-function analysis — to surface it, run `hotspots analyze . --per-function-touches` on a machine with sufficient memory.

### `(fill in)` — (fill in)

\{/* TODO: insight */\}

**Recommendation:** \{/* TODO: action */\}

## Key Takeaways

- \{/* TODO: takeaway 1 */\}
- \{/* TODO: takeaway 2 */\}
- \{/* TODO: takeaway 3 */\}


## Patterns Found

Antipatterns detected across the top functions in this snapshot:

| Pattern | Occurrences |
|---|---|
| `exit_heavy` | 5 |
| `god_function` | 5 |
| `complex_branching` | 4 |
| `long_function` | 4 |
| `deeply_nested` | 2 |
| `neighbor_risk` | 1 |

_These labels belong to two tiers — Tier 1 (structural): `complex_branching`, `deeply_nested`, `exit_heavy`, `long_function`, `god_function`. Tier 2 (relational/temporal): `hub_function`, `cyclic_hub`, `middle_man`, `neighbor_risk`, `stale_complex`, `churn_magnet`, `shotgun_target`, `volatile_god`._

## Reproduce This Analysis

```bash
git clone https://github.com/github/github-mcp-server
cd github-mcp-server
git checkout eff4c3c041742426f417f7c2247b96bbf6d60b69
hotspots analyze . --mode snapshot --explain-patterns --force --hybrid-touches 5
```

To run the same analysis on your own codebase, run `hotspots analyze . --mode snapshot` in any local git repo — no configuration required.

_I use Hotspots to highlight structural and activity risk — not "bad code." I treat these findings as a prioritization aid, not a bug predictor. [Editorial policy →](https://github.com/Stephen-Collins-tech/hotspots-content#editorial-policy)_
