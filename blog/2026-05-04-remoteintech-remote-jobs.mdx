---
title: "remote-jobs' link-fixing scripts carry the highest activity risk — 3 functions to address first"
titleSeo: "remoteintech/remote-jobs code health: fix-links.mjs leads refactoring priority with CC 38"
description: "Two critical-band functions in scripts/fix-links.mjs — classifyRedirect (CC 38) and classify (CC 21) — are both structurally complex and actively changing, making them live regression risks at commit fbbb50f."
introContent: "A single script file, fix-links.mjs, concentrates the highest structural and activity risk in remote-jobs — its top function carries a cyclomatic complexity of 38 while being touched right now."
pubDate: 2026-05-04
tags: ["oss", "javascript", "refactoring", "code-health"]
draft: true
repo: "remoteintech/remote-jobs"
commit: "fbbb50f2ab01b7996c55a9624d8f871bdbdc64d1"
language: "javascript"
hotspots_version: "1.16.1"
analyzed_at: "2026-05-04T06:19:03Z"
clone_depth: 100
topPatterns: ["exit_heavy", "neighbor_risk", "complex_branching", "deeply_nested"]
patternCounts: {exit_heavy: 3, neighbor_risk: 1, complex_branching: 1, deeply_nested: 1}
qaSection:
  - question: "What is an exit-heavy function and why does it matter in remote-jobs?"
    answer: "An exit-heavy function has many distinct return or early-exit points, meaning its behavior branches in multiple directions before completing. In remote-jobs, three of the top hotspots — including classifyRedirect and classify in scripts/fix-links.mjs — carry this pattern, which means a test suite must explicitly exercise each exit path or risk leaving real failure modes uncovered. The more return paths a function has, the easier it is to accidentally break one while editing another."
  - question: "How do I reduce cyclomatic complexity in JavaScript?"
    answer: "Extract each major branch or decision into a named helper function so that each unit of logic can be read and tested independently — for a function like classifyRedirect with a complexity of 38, grouping related conditions into well-named sub-functions can reduce the top-level complexity dramatically while making the branching intent explicit."
  - question: "Is remote-jobs actively maintained?"
    answer: "The data at commit fbbb50f shows that two top functions — classifyRedirect and classify, both in scripts/fix-links.mjs — recorded 1 touch each in the last 30 days and have a days_since_changed of 0, indicating the link-management scripting layer is under active development. The companies data layer, by contrast, has been dormant for 104 days, suggesting maintenance activity is currently concentrated in the scripting tooling rather than the site data pipeline."
  - question: "How do I reproduce this analysis?"
    answer: "Run the Hotspots CLI against the remoteintech/remote-jobs repository at commit fbbb50f to reproduce these results."
  - question: "What does activity-weighted risk mean?"
    answer: "Complexity × recent commit frequency — functions that are hard to understand AND actively changing are the highest priority for refactoring."
---

scripts/fix-links.mjs dominates the risk profile of remoteintech/remote-jobs: its top-ranked function, classifyRedirect, sits in the 'fire' quadrant with a cyclomatic complexity of 38 and a recent activity score of 13.08 — meaning it is both structurally complex and actively changing, making it a live regression risk rather than a backlog cleanup item. Across the full codebase of 70 analyzed functions, 6 are rated critical and 7 fall in the fire quadrant, with the concentration of that risk heavily skewed toward the link-management scripting layer. One additional critical function, an anonymous function in src/companies/companies.11tydata.js, has been untouched for 104 days and represents a different kind of risk: structural debt with high blast radius when next changed.

The table below ranks functions by **activity-weighted risk** — a score that multiplies structural complexity by recent commit frequency. A function that is both hard to understand (high cyclomatic complexity) and actively changing is a higher priority than one that is complex but untouched. CC = cyclomatic complexity (independent execution paths); ND = max nesting depth; FO = fan-out (distinct callees).

## Top 5 Hotspots

| Function | File | Risk | CC | ND | FO |
|---|---|---|---|---|---|
| `classifyRedirect` | scripts/fix-links.mjs | 18.7 | 38 | 2 | 9 |
| `classify` | scripts/fix-links.mjs | 16.5 | 21 | 2 | 3 |
| `reverify` | scripts/fix-links.mjs | 14.9 | 10 | 3 | 6 |
| `parseCsv` | scripts/fix-links.mjs | 14.7 | 14 | 6 | 6 |
| `isParked` | scripts/fix-links.mjs | 14.3 | 10 | 3 | 6 |

## Hotspot Analysis

### `classifyRedirect` — scripts/fix-links.mjs

Based on its name and file path, classifyRedirect likely inspects HTTP redirect responses and determines how each should be handled — deciding whether a link should be updated, skipped, flagged, or otherwise resolved. A cyclomatic complexity of 38 means there are 38 independent execution paths through this function, each one a potential bug surface and a required test case. It sits in the 'fire' quadrant with a recent activity score of 13.08 and 1 touch in the last 30 days (days_since_changed: 0), meaning structural complexity and active change are compounding each other right now. Its fan-out of 9 and the neighbor_risk pattern signal that changes here are likely to require coordinated edits across other parts of the codebase.

**Recommendation:** Add characterization tests that cover the distinct redirect outcomes before making any further changes, then extract each classification branch into a named sub-function to bring the CC down toward a manageable range. The neighbor_risk pattern means you should audit the 9 callee functions for downstream assumptions before refactoring.

### `classify` — scripts/fix-links.mjs

Also in scripts/fix-links.mjs, classify likely performs a broader categorization of link states — possibly acting as a dispatcher that delegates to classifyRedirect and other helpers. With a cyclomatic complexity of 21 and an exit_heavy pattern, it has numerous return paths, each of which must be independently tested to ensure coverage. Like classifyRedirect, it is in the 'fire' quadrant with a recent activity score of 11.46 and was last changed 0 days ago, confirming it is actively in flux. Its fan-out of 3 is lower, but given that it likely orchestrates other classification logic, changes here carry outsized behavioral risk.

**Recommendation:** Map the multiple exit paths explicitly — a decision table or enumerated return-value type can make the branching contract visible — and enforce that each path has at least one test case before the next commit lands.

### `<anonymous>` — src/companies/companies.11tydata.js

This anonymous function lives in the Eleventy data layer for the companies section of the site, where it likely assembles or transforms company records for template consumption. Despite having a moderate cyclomatic complexity of 12 and a fan-out of 11 — the highest fan-out in the top hotspots — it has not been touched in 104 days and records 0 touches in the last 30 days, placing it firmly in the 'debt' quadrant. The combination of 11 distinct callees and the exit_heavy pattern means that when this function is next modified, the blast radius will be broad and the multiple exit paths will make it easy to miss a branch during testing.

**Recommendation:** Before the next development push that touches the companies data pipeline, give this function a name, add integration tests that cover its exit paths, and consider decomposing the 11-callee fan-out into smaller, independently testable data-transformation steps.

### `isLocaleOnly` — scripts/fix-links.mjs

isLocaleOnly likely checks whether a given URL or redirect target differs only by locale — a narrow, well-scoped predicate. Its cyclomatic complexity of 6 and max nesting depth of 1 confirm the logic is straightforward, and its fan-out of 2 means it has minimal coupling. It is in the 'watch' quadrant with 1 touch in the last 30 days, meaning it is active but carries no structural alarm. The moderate activity_risk of 10.24 is driven primarily by its recent activity score of 5.96 rather than structural complexity.

**Recommendation:** No refactoring is warranted here — monitor it alongside classifyRedirect and classify since they share the same file, and ensure its test coverage is updated if the redirect classification logic around it changes.

## Patterns Found

Antipatterns detected across the top functions in this snapshot:

| Pattern | Occurrences |
|---|---|
| `exit_heavy` | 3 |
| `neighbor_risk` | 1 |
| `complex_branching` | 1 |
| `deeply_nested` | 1 |

_These labels belong to two tiers — Tier 1 (structural): `complex_branching`, `deeply_nested`, `exit_heavy`, `long_function`, `god_function`. Tier 2 (relational/temporal): `hub_function`, `cyclic_hub`, `middle_man`, `neighbor_risk`, `stale_complex`, `churn_magnet`, `shotgun_target`, `volatile_god`._

## Key Takeaways

- classifyRedirect in scripts/fix-links.mjs has a cyclomatic complexity of 38 and was modified within the last 0 days — add characterization tests before any further changes to avoid introducing regressions across its 38 execution paths.
- The anonymous function in src/companies/companies.11tydata.js has a fan-out of 11 and has been untouched for 104 days — give it a name and add tests now, before the next development push triggers its high blast radius.
- Three of the top five hotspots share the exit_heavy pattern, meaning test suites need explicit coverage for each return path in classifyRedirect, classify, and the companies.11tydata.js anonymous function — not just the happy path.

## Reproduce This Analysis

```bash
git clone https://github.com/remoteintech/remote-jobs
cd remote-jobs
git checkout fbbb50f2ab01b7996c55a9624d8f871bdbdc64d1
hotspots analyze . --mode snapshot --explain-patterns --force
```

To run the same analysis on your own codebase, run `hotspots analyze . --mode snapshot` in any local git repo — no configuration required.

_Hotspots highlights structural and activity risk — not "bad code." Findings are a prioritization aid, not a bug predictor. [Editorial policy →](https://github.com/Stephen-Collins-tech/hotspots-content#editorial-policy)_
