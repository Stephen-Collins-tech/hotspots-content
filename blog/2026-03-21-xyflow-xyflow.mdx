---
title: "xyflow's drag and resize layer carries the highest activity risk — 5 functions to address first"
titleSeo: "xyflow technical debt: XYDrag and XYResizer lead refactoring priority in xyflow/xyflow"
description: "XYDrag and XYResizer in xyflow/xyflow dominate structural risk with CC 42 and CC 23 respectively, both flagged as god functions with fan-out above 20 — high blast radius when next touched."
introContent: "xyflow's drag system carries a cyclomatic complexity of 42 and calls 32 distinct functions — structural debt with a wide blast radius sitting one refactor away from regression."
pubDate: 2026-03-21
tags: ["oss", "typescript", "refactoring", "code-health"]
draft: true
repo: "xyflow/xyflow"
commit: "916c557144c6da2eeb376d5b61701099aa9abe03"
language: "typescript"
hotspots_version: "1.8.1"
analyzed_at: "2026-03-21T16:33:20Z"
clone_depth: 25
topPatterns: ["long_function", "god_function", "exit_heavy", "complex_branching", "hub_function"]
patternCounts: {exit_heavy: 6, god_function: 10, long_function: 11, complex_branching: 2, hub_function: 1}
qaSection:
  - question: "What is a god function and why does it matter in xyflow?"
    answer: "A god function is one that takes on too many responsibilities — it handles initialization, branching logic, state mutation, and coordination all in one place. In xyflow, `XYDrag` and `useReactFlow` are both flagged as god functions, which matters because any single change must navigate the full complexity of the function, and with fan-outs of 32 and 37 respectively, a mistake can propagate through dozens of downstream dependencies before surfacing as a bug."
  - question: "How do I reduce fan-out in TypeScript?"
    answer: "The most direct technique is the extract-method refactor: pull cohesive groups of callees into dedicated collaborator classes or composable hooks, so each unit calls only the dependencies it actually owns. For hooks like `useReactFlow`, splitting one large hook into several scoped hooks (e.g., one for node state, one for edge state, one for viewport control) naturally partitions the dependency graph and makes each piece independently testable."
  - question: "Is xyflow actively maintained?"
    answer: "The structural complexity in xyflow's top functions is real, but all five top-ranked hotspots sit in the 'debt' quadrant — meaning they are not currently under active churn. The recent activity scores (ranging from 12.24 to 15.05) reflect historical commit weight, not ongoing daily changes, so the risk today is accumulated structural debt rather than live regression pressure."
  - question: "How do I reproduce this analysis?"
    answer: "Run the Hotspots CLI against the xyflow/xyflow repository at commit 916c557 to reproduce these exact scores and rankings."
  - question: "What does activity-weighted risk mean?"
    answer: "Complexity × recent commit frequency — functions that are hard to understand AND actively changing are the highest priority for refactoring."
---

Across 1,967 analyzed functions in xyflow/xyflow, 40 are rated critical — and the highest-risk cluster sits entirely in the drag, resize, and store subsystems. The top-ranked function, `XYDrag`, carries a cyclomatic complexity of 42 and fans out to 32 distinct callees; it sits in the 'debt' quadrant, meaning it has not been actively touched recently but represents structural debt with an outsized blast radius the moment development resumes. xyflow is the framework-agnostic flow diagram library powering React Flow and Svelte Flow, and with 40 critical-band functions across its packages, the structural risk is concentrated — not spread thin.

The table below ranks functions by **activity-weighted risk** — a score that multiplies structural complexity by recent commit frequency. A function that is both hard to understand (high cyclomatic complexity) and actively changing is a higher priority than one that is complex but untouched. CC = cyclomatic complexity (independent execution paths); ND = max nesting depth; FO = fan-out (distinct callees).

## Top 5 Hotspots

| Function | File | Risk | CC | ND | FO |
|---|---|---|---|---|---|
| `XYDrag` | packages/system/src/xydrag/XYDrag.ts | 15.9 | 42 | 3 | 32 |
| `update` | packages/system/src/xydrag/XYDrag.ts | 15.1 | 42 | 3 | 32 |
| `useReactFlow` | packages/react/src/hooks/useReactFlow.ts | 14.4 | 16 | 1 | 37 |
| `XYResizer` | packages/system/src/xyresizer/XYResizer.ts | 14.1 | 23 | 4 | 20 |
| `createStore` | packages/svelte/src/lib/store/index.ts | 14.1 | 17 | 3 | 36 |

_Hotspots highlights structural and activity risk — not "bad code." Findings are a prioritization aid, not a bug predictor. [Editorial policy →](https://github.com/Stephen-Collins-tech/hotspots-content#editorial-policy)_
