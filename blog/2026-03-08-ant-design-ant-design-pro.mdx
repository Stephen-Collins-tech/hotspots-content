---
title: "ant-design-pro's request and mock layers carry the highest activity risk — 4 functions to address first"
titleSeo: "ant-design-pro code health: request error config and mock layer lead refactoring priority"
description: "In ant-design-pro, the anonymous handler in requestErrorConfig.ts and two mock endpoint functions in listTableList.ts dominate the critical risk band, combining high cyclomatic complexity with the repo's heaviest recent commit churn."
introContent: "The error-handling config in ant-design-pro is both the most structurally complex and the most actively changed file — a combination that makes it a live regression risk, not just a cleanup item."
pubDate: 2026-03-08
tags: ["oss", "typescript", "refactoring", "code-health"]
draft: false
repo: "ant-design/ant-design-pro"
commit: "607e63f4fdb49d78306a618f2b2c29291ce85500"
language: "typescript"
report_html: "https://reports.hotspots.dev/ant-design/ant-design-pro/607e63f4fdb49d78306a618f2b2c29291ce85500/index.html"
report_json: "https://reports.hotspots.dev/ant-design/ant-design-pro/607e63f4fdb49d78306a618f2b2c29291ce85500/snapshot.json"
hotspots_version: "1.5.0"
analyzed_at: ""
clone_depth: 25
topPatterns: ["exit_heavy", "god_function", "long_function"]
patternCounts: {exit_heavy: 3, god_function: 3, long_function: 2}
qaSection:
  - question: "What is a god function and why does it matter in ant-design-pro?"
    answer: "A god function is one that takes on too many responsibilities — coordinating validation, data transformation, state changes, and responses all in one place. In ant-design-pro, both postRule in the mock layer and the anonymous function in the table-list page carry this pattern, which means a change to any one concern risks breaking the others and makes the function harder to test in isolation."
  - question: "How do I reduce fan-out in TypeScript?"
    answer: "Extract groups of related callees into focused helper functions or custom hooks so that each unit calls only the dependencies relevant to its single responsibility; in the table-list anonymous function, separating data-fetching calls from rendering callbacks alone would meaningfully reduce its fan-out of 19."
  - question: "Is ant-design-pro actively maintained?"
    answer: "The activity data strongly suggests so: the top four functions all carry recent activity scores above 10, reflecting high recent commit frequency, and four functions have reached the critical risk band at commit 607e63f — a pattern consistent with an actively developed codebase, not a stagnant one."
  - question: "How do I reproduce this analysis?"
    answer: "Run the hotspots CLI against the ant-design/ant-design-pro repository at commit 607e63f using your hotspots.dev configuration."
  - question: "What does activity-weighted risk mean?"
    answer: "Complexity × recent commit frequency — functions that are hard to understand AND actively changing are the highest priority for refactoring."
---

Across 162 functions in ant-design/ant-design-pro, 4 have reached critical band status — and the risk is concentrated in two layers: HTTP error handling and mock data endpoints. The anonymous function in src/requestErrorConfig.ts leads with an activity risk of 12.12 and a recent activity score of 12.12, meaning it is not merely complex (CC 19) but is actively receiving commits right now, turning every change into a live regression surface rather than a deferred cleanup task. ant-design-pro is Ant Design's enterprise-grade React scaffolding template, and at this commit (607e63f), its critical hotspots cluster around request lifecycle management and mock API simulation — the seams most likely to break under active feature development.

The table below ranks functions by **activity-weighted risk** — a score that multiplies structural complexity by recent commit frequency. A function that is both hard to understand (high cyclomatic complexity) and actively changing is a higher priority than one that is complex but untouched. CC = cyclomatic complexity (independent execution paths); ND = max nesting depth; FO = fan-out (distinct callees); LRS = recent activity score (commit frequency × recency, independent of complexity).

## Top 5 Hotspots

| Function | File | Risk | CC | ND | FO |
|---|---|---|---|---|---|
| `<anonymous>` | src/requestErrorConfig.ts | 12.1 | 19 | 3 | 3 |
| `postRule` | mock/listTableList.ts | 11.8 | 13 | 2 | 11 |
| `<anonymous>` | src/pages/table-list/index.tsx | 10.7 | 6 | 1 | 19 |
| `getRule` | mock/listTableList.ts | 10.1 | 11 | 3 | 9 |
| `<anonymous>` | src/pages/user/login/index.tsx | 8.5 | 12 | 2 | 17 |

## Hotspot Analysis

### `<anonymous>` — src/requestErrorConfig.ts

Based on its file path, this anonymous function almost certainly defines the central HTTP error-handling configuration — intercepting responses, classifying error codes, and dispatching user-facing messages. A cyclomatic complexity of 19 means there are at least 19 independent execution paths through this logic, each a distinct error scenario that requires its own test case. The exit_heavy pattern compounds this: multiple return paths make it difficult to reason about which branch executes under which condition, and with a recent activity score of 12.12 — the highest in the repo — every recent commit to this file risks silently breaking an untested path.

**Recommendation:** Before any further changes, add characterization tests that cover each of the 19 logical branches, then extract distinct error-classification cases (e.g. network errors, auth failures, server errors) into named handler functions to bring CC below 10.

### `postRule` — mock/listTableList.ts

The name and file path indicate this function handles POST requests in the mock table-list API — likely validating input, mutating mock state, and returning a shaped response. Its fan-out of 11 is the telling metric: calling 11 distinct functions from a single mock handler means it is acting as a coordination hub, and the god_function pattern confirms this — changes here can ripple across a wide surface of mock utilities and shared state. CC 13 with an exit_heavy pattern means there are multiple conditional return paths through that coordination logic, and a recent activity score of 11.76 places it squarely in active development right now.

**Recommendation:** Decompose postRule by extracting validation, state mutation, and response-shaping into separate, independently testable functions; this will reduce both CC and fan-out and shrink the blast radius of future changes.

### `<anonymous>` — src/pages/table-list/index.tsx

This anonymous function lives in the table-list page component and, given the long_function and god_function patterns, likely handles the full lifecycle of the table view — data fetching, column configuration, action callbacks, and render logic — all in one place. Its fan-out of 19 is the highest in the critical band and is the dominant structural risk: with 19 distinct callees, a change to any one of those dependencies could require a corresponding update here, making this function a shotgun target. The quadrant classification of 'fire' — meaning both complex and actively changing — is reinforced by an activity risk of 10.73 against a recent activity score of 9.7.

**Recommendation:** Apply extract-method refactoring to separate data-fetching logic, column definitions, and action handlers into dedicated hooks or sub-components; auditing all 19 fan-out targets first will clarify which extractions deliver the most coupling reduction.

## Patterns Found

Antipatterns detected across the top functions in this snapshot:

| Pattern | Occurrences |
|---|---|
| `exit_heavy` | 3 |
| `god_function` | 3 |
| `long_function` | 2 |

_These labels belong to two tiers — Tier 1 (structural): `complex_branching`, `deeply_nested`, `exit_heavy`, `long_function`, `god_function`. Tier 2 (relational/temporal): `hub_function`, `cyclic_hub`, `middle_man`, `neighbor_risk`, `stale_complex`, `churn_magnet`, `shotgun_target`, `volatile_god`._

## Key Takeaways

- The anonymous function in src/requestErrorConfig.ts has a recent activity score of 12.12 — the highest in the repo — meaning it is the single most urgent file to test and stabilize before the next feature sprint; its CC of 19 requires at least 19 test cases to cover all paths.
- postRule in mock/listTableList.ts has a fan-out of 11 and is tagged as a god_function: any refactoring of shared mock utilities must account for this function as a high-blast-radius dependency hub.
- The table-list page component's anonymous function reaches a fan-out of 19 and sits in the 'fire' quadrant — both complex and actively changing — making it the strongest candidate for an extract-method pass to isolate data-fetching, column configuration, and action handling into separate units.

## Reproduce This Analysis

```bash
git clone https://github.com/ant-design/ant-design-pro
cd ant-design-pro
git checkout 607e63f4fdb49d78306a618f2b2c29291ce85500
hotspots analyze . --mode snapshot --explain-patterns --force
```

To run the same analysis on your own codebase, run `hotspots analyze . --mode snapshot` in any local git repo — no configuration required.

_Hotspots highlights structural and activity risk — not "bad code." Findings are a prioritization aid, not a bug predictor. [Editorial policy →](https://github.com/Stephen-Collins-tech/hotspots-content#editorial-policy)_
